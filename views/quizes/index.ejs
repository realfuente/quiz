<table>
<% if ( quizes.length === 0 ) { %>
	 <tr><td>No hay resultados</td></tr>
<% } else {
	var i; %>
	<th>Tema</th>
	<th>Pregunta</th>
	
<%	for (i=0; i < quizes.length; i++) {
%> 	
   <tr>
   <td><%= quizes[i].tema %></td>
   <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
   <td><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
      <td>
        <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
          <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
            borrar
          </button>
        </form>
    </td>   
   </tr> 
<% } } %>
</table><p>
<div id="botoncrear"><a href="/quizes/new"><button type="button">Crear pregunta</button></a></div>
<form id="buscadorpreguntas" method="get" action="">
			Buscador de Preguntas:
			<input type="text" name="search" value="Escriba la pregunta"/>
			<input type="submit" value="Buscar">
</form>

